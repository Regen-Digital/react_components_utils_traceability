interface IHeader {
    routerLinks: {
        title: string;
        path: string;
    }[];
}
interface IHeader {
    routerLinks: {
        title: string;
        path: string;
    }[];
}
/**
 * Header component is used to display the header and navigation to other pages
 */
declare const Header: ({ routerLinks }: IHeader) => import("react/jsx-runtime").JSX.Element;
export default Header;
